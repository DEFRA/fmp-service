const Wreck = require('@hapi/wreck');
const fs = require('fs');
async function body(rawdataASArray) {
    try {
        var content = [
        ];
        return content;
    } catch (error) {
        return error;
    }
};
module.exports = body;